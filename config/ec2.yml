runtime:
  sample_mode: false
  sample_row_limit: null

spark:
  app_name_prefix: anti-scraping-ec2
  master: local[*]
  shuffle_partitions: 32
  driver_memory: 8g
  executor_memory: 8g
  default_parallelism: 8

paths:
  base_uri: s3a://anti-scraping-soomin/anti-scraping
  raw_text_input: /opt/anti-scraping/data/access.log
  normalized_events: normalized_events
  quarantined_raw_events: quarantined_raw_events
  sessionized_events: sessionized_events
  session_features: session_features
  suspicious_sessions: suspicious_sessions
  daily_abuse_summary: daily_abuse_summary
  daily_abuse_summary_sample_json: daily_abuse_summary_sample_json
